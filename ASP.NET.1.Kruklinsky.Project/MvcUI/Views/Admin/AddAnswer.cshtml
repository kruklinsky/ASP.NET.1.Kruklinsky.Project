@model MvcUI.Models.Answer

@{
    Layout = null;
}

<div class="stroke">
    @using (Html.BeginForm("AddAnswer", "Admin", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <label>Add new answer</label>
        @Html.EditorForModel();
        <br />
        <input class="btn btn-inverse" type="submit" value="Add" />
    }
</div>
